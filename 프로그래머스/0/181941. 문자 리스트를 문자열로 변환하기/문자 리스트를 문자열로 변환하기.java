class Solution {
    public String solution(String[] arr) {
        String answer = "";
        
        for (int l = 0; l < arr.length; l++){
            answer += arr[l];
        }
        return answer;
    }
}